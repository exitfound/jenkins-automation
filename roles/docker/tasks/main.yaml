---
- include: debian.yaml
  when: ansible_os_family == 'Debian'
  tags: docker

- include: redhat.yaml
  when: ansible_os_family == 'RedHat'
  tags: docker

- include: centos.yaml
  when: ansible_os_family == 'RedHat'
  tags: docker

- include: suse.yaml
  when: ansible_os_family == 'Suse'
  tags: docker
