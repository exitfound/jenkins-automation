server {
     listen       80 default_server;
     listen       [::]:80 default_server;
     server_name  {{ jenkins_nginx_domain }};

     location / {
         proxy_pass 	http://localhost:8080;
     }
}

# upstream jenkins {
#     server 127.0.0.1:8080;
# }

# server {

#     server_name  {{ jenkins_nginx_domain }}; # managed by Certbot

#     location / {
#         sendfile off;
#         proxy_pass         http://jenkins;
#         proxy_redirect     http://localhost:8080 https://{{ jenkins_nginx_domain }};

#         proxy_set_header   X-Forwarded-Host  $host;
#         proxy_set_header   X-Forwarded-Port  $server_port;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_max_temp_file_size 0;
#     }

#     listen [::]:443 ssl ipv6only=on; # managed by Certbot
#     listen 443 ssl; # managed by Certbot
#     ssl_certificate /etc/letsencrypt/live/{{ jenkins_nginx_domain }}/fullchain.pem; # managed by Certbot
#     ssl_certificate_key /etc/letsencrypt/live/{{ jenkins_nginx_domain }}/privkey.pem; # managed by Certbot
#     include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
#     }

# server {
#     if ($host = {{ jenkins_nginx_domain }} ) {
#         return 301 https://$host$request_uri;
#     } # managed by Certbot

#     listen       80 default_server;
#     listen       [::]:80 default_server;
#     server_name  {{ jenkins_nginx_domain }};
#     return 404; # managed by Certbot
#     }

# server {
#     if ($host = www.{{ jenkins_nginx_domain }} ) {
#         return 301 https://$host$request_uri;
#     } # managed by Certbot

#     listen       80;
#     listen       [::]:80;
#     server_name www.{{ jenkins_nginx_domain }};
#     return 404; # managed by Certbot
#     }
